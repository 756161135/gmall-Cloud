spring:
  application:
    name: GMALL-MANAGE
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    appname: GMALL-MANAGE
    prefer-ip-address: true
feign:
  hystrix:
    enabled: true
server:
  port: 8081